clear all
close all
clc

eps = 10^-10;
for i = 1:1000
    A = 100*randn(5);
    A = A+A';
    [L,D] = mcfac(A);
    C = L*D*L';
    for k = size(A,1)
        for j = size(A,2)
            if (A(k,j) > C(k,j)+eps) || ((A(k,j) < C(k,j)-eps)) 
                disp('erro');
                return
            end
        end
    end
    v = ei
    for k = 1:size(A,1)
        if ()
    i
end